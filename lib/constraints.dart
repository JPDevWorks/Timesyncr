import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBackground = Color(0xFF000000); // Update with your color
  static const Color buttonBackground = Color(0xFFFFFFFF); // Update with your color
  static const Color buttonText = Color(0xFF000000); // Update with your color
  static const Color textColor = Color(0xFFFFFFFF); // Update with your color
  static const Color linkText = Color(0xFF00FF00); // Update with your color
}